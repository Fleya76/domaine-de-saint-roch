{{ form_start(form) }}
    {{ form_row(form.title, {'attr' : {'class' : 'form-control'}}) }}
    <button class="mt-3 mb-3 btn btn-success">Enregistrer la catégorie</button>
    <a href="{{ path('category_index') }}">Voir les catégories</a>
{{ form_end(form) }}
