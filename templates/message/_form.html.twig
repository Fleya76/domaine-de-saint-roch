{{ form_start(form) }}
    {# TODO: Décommenter le chien #}
    {# {{ form_row(form.dog, {'attr': {'class': 'form-control'}}) }} #}
    {{ form_row(form.subject, {'attr': {'class': 'form-control'}}) }}
    {{ form_row(form.content, {'attr': {'class': 'form-control'}}) }}

    <button class="mt-3 mb-3 btn btn-success">Envoyer le message</button>
{{ form_end(form) }}